---
title: Reference
description: "Configuration options with defaults, the deploy contract, and the conventions the UI and API keep."
nav_order: 6
has_children: true
---

# Reference

Every configuration option with defaults, the deploy contract, and the
conventions the UI and API keep.
